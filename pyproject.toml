[build-system]
# >=1.27 guarantees PEP 639 (License-Expression + License-File) emission.
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"

[project]
name = "cursor-theater"
dynamic = ["version"]
description = "Watch your Cursor agent conversations work — a live office for every project."
readme = "README.md"
requires-python = ">=3.9"
license = "MIT"
license-files = ["LICENSE"]
authors = [{ name = "udah1" }]
keywords = ["cursor", "cursor-ai", "agents", "visualizer", "dashboard", "dev-tools"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Environment :: Console",
    "Environment :: Web Environment",
    "Intended Audience :: Developers",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Software Development",
    "Topic :: Utilities",
]
# Pure standard library — no runtime dependencies.
dependencies = []

[project.urls]
Homepage = "https://github.com/udah1/cursor-theater"
Issues = "https://github.com/udah1/cursor-theater/issues"

[project.scripts]
cursor-theater = "cursor_theater:main"

# Version is the single source of truth in cursor_theater.py (__version__).
[tool.hatch.version]
path = "cursor_theater.py"

# Single top-level module — ship exactly that one file in the wheel.
[tool.hatch.build.targets.wheel]
include = ["cursor_theater.py"]

[tool.hatch.build.targets.sdist]
include = [
    "cursor_theater.py",
    "start.cmd",
    "README.md",
    "LICENSE",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "ROADMAP.md",
    "docs/",
]

[tool.ruff]
target-version = "py39"
line-length = 120

[tool.ruff.lint]
# Catch real bugs (pyflakes + syntax), not style bikeshedding.
select = ["E9", "F"]
